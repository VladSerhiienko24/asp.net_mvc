@model Tours_1._0.Models.TypeTour

@{
    ViewBag.Title = "Удаление";
}

<h3>Вы уверены, что хотите удалить эту категорию?</h3>

<div>
    <h4>TypeTour</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.TypeTourName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.TypeTourName)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

    <div class="form-actions no-color">
        @Html.ActionLink("Вернуться назад", "Index", null, new { @class = "btn btn-default" })
        <input type="submit" value="Удалить" class="btn btn-default" />
    </div>
    }
</div>
